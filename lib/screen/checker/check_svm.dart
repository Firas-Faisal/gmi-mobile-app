import 'package:flutter/material.dart';

class SVMChecker extends StatelessWidget {
  const SVMChecker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Center(child: Text('Check SVM'))],
    );
  }
}
