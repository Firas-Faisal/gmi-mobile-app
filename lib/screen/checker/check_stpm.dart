import 'package:flutter/material.dart';

class STPMChecker extends StatelessWidget {
  const STPMChecker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Center(child: Text('Check STPM'))],
    );
  }
}
